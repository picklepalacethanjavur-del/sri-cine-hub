"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";import {DocumentActions} from "@/components/DocumentActions";
export function SupplierRFQActions({id,code,status,supplierName}:{id:string;code:string;status:string;supplierName:string}){const supabase=createClient();const router=useRouter();const [busy,setBusy]=useState(false);async function mark(next:string){setBusy(true);const {error}=await supabase.from("supplier_rfqs").update({status:next,updated_at:new Date().toISOString()}).eq("id",id);setBusy(false);if(error)alert(error.message);else router.refresh();}return <><div className="documentStatusLine printHide"><span>STATUS</span><b className={`workflowBadge ${status}`}>{status.replaceAll("_"," ")}</b></div><DocumentActions editHref="/admin/supplier-rfqs/new" downloadHref={`/api/documents/supplier-rfq/${id}/pdf`} fileName={`SriCineHub-${code}.pdf`} whatsappText={`Hi ${supplierName}, please find Sri Cine Hub ${code}. Kindly confirm equipment availability and your best rental rates.`}>{(status==="generated"||status==="draft")&&<button className="button gold" disabled={busy} onClick={()=>mark("sent")}>Mark Sent</button>}{status==="sent"&&<button className="button gold" disabled={busy} onClick={()=>mark("quote_received")}>Quote Received</button>}{status==="quote_received"&&<><button className="button ghost dangerButton" disabled={busy} onClick={()=>mark("declined")}>Decline</button><button className="button gold" disabled={busy} onClick={()=>mark("confirmed")}>Confirm Supplier</button></>}</DocumentActions></>;}
