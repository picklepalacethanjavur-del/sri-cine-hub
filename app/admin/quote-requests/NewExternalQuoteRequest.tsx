"use client";
import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {DateTimePicker} from "@/components/DateTimePicker";
export function NewExternalQuoteRequest({userId}:{userId:string}){
 const supabase=createClient();const [open,setOpen]=useState(false);const [start,setStart]=useState("");const [end,setEnd]=useState("");const [busy,setBusy]=useState(false);const [message,setMessage]=useState("");
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(busy)return;if(start&&end&&new Date(end)<=new Date(start)){setMessage("Return must be after start when both dates are entered.");return;}setBusy(true);setMessage("");const form=e.currentTarget;try{const f=new FormData(form);const {data:req,error}=await supabase.from("quote_requests").insert({name:String(f.get("name")||"").trim()||null,company_name:String(f.get("company")||"").trim()||null,phone:String(f.get("phone")||"").trim()||null,project_name:String(f.get("project")||"").trim()||null,start_at:start?new Date(start).toISOString():null,end_at:end?new Date(end).toISOString():null,notes:String(f.get("notes")||"").trim()||null,status:"new"}).select("id,request_code").single();if(error)throw error;const files=f.getAll("requestFiles").filter(x=>x instanceof File&&x.size) as File[];const failed:string[]=[];for(const file of files){try{if(file.size>25*1024*1024)throw new Error("File exceeds 25 MB");const safe=file.name.replace(/[^a-zA-Z0-9._-]/g,"_");const path=`${req.id}/${Date.now()}-${crypto.randomUUID().slice(0,8)}-${safe}`;const up=await supabase.storage.from("quote-request-documents").upload(path,file,{contentType:file.type||undefined,upsert:false});if(up.error)throw up.error;const meta=await supabase.from("quote_request_attachments").insert({quote_request_id:req.id,file_name:file.name,file_path:path,content_type:file.type||null,file_size:file.size,uploaded_by:userId});if(meta.error)throw meta.error;}catch{failed.push(file.name);}}if(failed.length)sessionStorage.setItem("quoteRequestUploadWarning",`Request created, but upload again: ${failed.join(", ")}`);location.href=`/admin/quote-requests/${req.id}`;}catch(err){setMessage(err instanceof Error?err.message:"Unable to create quote request.");setBusy(false);}}
 return <div className="newExternalRequestWrap"><button className="button gold" type="button" onClick={()=>setOpen(v=>!v)}>{open?"Close":"+ New Request"}</button>{open&&<form className="adminPanel externalRequestForm" onSubmit={submit}><div className="panelHeading"><div><h2>Start a quotation request</h2><p>Everything is optional. Create the workspace first, then add customer details, dates, files and items as they arrive.</p></div></div><div className="formGrid"><label>Production / Client<input name="company"/></label><label>Project<input name="project"/></label></div><div className="formGrid"><label>Contact name<input name="name"/></label><label>Phone / WhatsApp<input name="phone"/></label></div><div className="formGrid"><DateTimePicker label="Start (optional)" value={start} onChange={setStart}/><DateTimePicker label="Return (optional)" value={end} min={start} onChange={setEnd}/></div><label>Original request document(s)<input name="requestFiles" type="file" multiple accept=".pdf,.png,.jpg,.jpeg,.webp,.xlsx,.xls,.csv,.doc,.docx,application/pdf,image/*"/></label><label>Internal note<textarea name="notes" rows={3} placeholder="Optional"/></label><div className="formActions"><button type="submit" className="button gold" disabled={busy}>{busy?"Creating…":"Create Request & Open Builder"}</button></div>{message&&<div className="errorBox">{message}</div>}{busy&&<div className="actionOverlay"><div className="actionOverlayCard"><span className="loadingSpinner"/><b>Creating request…</b></div></div>}</form>}</div>;
}
