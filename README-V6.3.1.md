# Sri Cine Hub V6.3.1 — PDF Table Header Fix

- Receipt PDF now uses only `Description | Amount`; removed meaningless Qty/Days/Rate columns and repeated `1` values.
- Customer quotation PDF now explicitly renders `S.No | Description | Qty | Days | Rate | Amount` headers.
- Supplier RFQ PDF now renders `S.No | Description | Qty | Days` headers.
- Shared PDF generator now supports document-specific table layouts.
